#!/bin/bash

script_dir=$(dirname "$(realpath "$0")")

. "$script_dir/nvim_library.sh"

env_args=()
if [ -n "${NVIM_APPNAME:-}" ]; then
  env_args+=(--env "NVIM_APPNAME=${NVIM_APPNAME}")
fi

# Launch Singularity
"$script_dir/ct_exec.sh" "--$TOOL" "${env_args[@]}" "$NVIM_CONT_LOCATION" nvim "$@"
